node_modules/
dist/
.next/
*.tsbuildinfo
next-env.d.ts

# Environment
# Each app is configured by a single .env; only the .example files are tracked.
.env

# Logs
*.log
npm-debug.log*

# Test / coverage
coverage/

# Editor / OS
.DS_Store
.idea/

# Personal editor state stays out, but the shared extension recommendations
# are tracked — they are only useful if everyone gets them.
.vscode/*
!.vscode/extensions.json
